# 长隆旅游网站 - 开发任务清单

> 依据《开发设计文档》整理，用于指导日常开发。按阶段顺序执行，可勾选 `[x]` 标记完成。

---

## 阶段一：项目骨架搭建

- [x] 创建目录结构：`css/`、`js/`、`assets/images/`、`assets/data/`、`assets/icons/`
- [ ] 创建 `base.css`：`:root` 变量（主色 #1E3A8A、暖米色 #F5F0E1、辅助色 #E2D9C8）、基础重置、字体
- [ ] 创建 `layout.css`：容器最大宽度、Flex/Grid 布局类、响应式断点 768px
- [ ] 创建 `components.css`：占位骨架（后续补充）
- [ ] 创建 `index.html`：语义化结构（header/nav/main/section/footer），引入 Tailwind CDN、base/layout/components
- [ ] 创建 `destinations.html`：基础页面骨架
- [ ] 创建 `guide.html`：基础页面骨架
- [ ] 创建 `analytics.html`：基础页面骨架
- [ ] 验证：4 个页面可访问、布局正常

---

## 阶段二：布局与导航

- [ ] 实现 `nav.js`：`initNav()` 函数
- [ ] 导航：滚动 > 50px 时背景透明→实色（scrolled 类）
- [ ] 导航：选中/悬停项下划线从左到右滑动（::after + scaleX）
- [ ] 导航：下拉菜单（如目的地分类）缓动展开
- [ ] 导航：移动端（≤768px）汉堡菜单，点击展开/收起
- [ ] 在各 HTML 中引入 `nav.js` 并调用
- [ ] 统一 footer：版权、联系方式，各页复用
- [ ] 页面间跳转链接：首页→目的地/攻略/数据可视化
- [ ] 响应式：PC/平板/手机布局自适应，无横向滚动条

---

## 阶段三：首页核心交互

- [ ] 创建 `utils.js`：`debounce`、`throttle`、`scrollToTop`、`getQueryParam`
- [ ] 创建 `carousel.js`：`initCarousel(containerSelector, options)`
- [ ] 轮播：无缝自动轮播（克隆首尾 slide）
- [ ] 轮播：鼠标悬停暂停
- [ ] 轮播：左右箭头手动切换
- [ ] 轮播：指示器 + 进度条动画
- [ ] 轮播：切换时淡入淡出 + 轻微缩放过渡
- [ ] 首页轮播区：3~5 张图，调用 `initCarousel`
- [ ] 创建 `assets/data/destinations.json`：6~8 条目的地数据
- [ ] 首页目的地概览：Grid 布局渲染卡片
- [ ] 目的地卡片样式：悬停上移 5px、阴影加深、图片放大、文字渐显
- [ ] 首页入口卡片：数据可视化、攻略入口链接
- [ ] 创建 `back-to-top.js`：`initBackToTop(buttonSelector)`
- [ ] 回到顶部：滚动 > 500px 渐显，点击平滑回顶，悬停旋转+变色
- [ ] 在各页引入 back-to-top 并添加按钮

---

## 阶段四：目的地列表与数据可视化

- [ ] 创建 `search.js`：`initSearch(inputSelector, resultSelector, dataSource)`
- [ ] 搜索：输入防抖 300ms
- [ ] 搜索：实时联想匹配 destinations
- [ ] 搜索：结果打字机效果逐个显示
- [ ] 目的地列表页：搜索区 + 列表区（卡片网格）
- [ ] 目的地列表页：从 `destinations.json` 动态渲染
- [ ] 创建 `assets/data/chart-data.json`：cityVisit、travelMode、hotTrend
- [ ] 创建 `charts.js`：引入 ECharts CDN
- [ ] `initBarChart(containerId, dimension)`：柱状图，月/季/年切换
- [ ] `initPieChart(containerId)`：饼图，图例筛选、点击扇区放大
- [ ] `initLineChart(containerId)`：折线图，动画从 0 增长（加分）
- [ ] `analytics.html`：柱状图 + 饼图 + 折线图容器，维度切换按钮
- [ ] 柱状图：悬停显示数值 + 高亮
- [ ] 饼图：`selectedMode: 'single'` 实现点击放大

---

## 阶段五：攻略详情页

- [ ] 创建 `assets/data/guides.json`：攻略数据（含 id、title、content、coverUrl、relatedIds）
- [ ] 攻略页：根据 `?id=` 参数加载对应攻略
- [ ] 攻略页：标题区、封面图
- [ ] 攻略页：正文区（article）、图片、视频，响应式适配
- [ ] 攻略页：相关推荐（根据 relatedIds 渲染其他攻略卡片）
- [ ] 表单区：预订/咨询表单，美观样式
- [ ] 3D 相册（加分）：`transform-style: preserve-3d`，鼠标移动时 rotateX/Y 跟随
- [ ] 表格：若有数据展示需求，设计美观表格样式

---

## 阶段六：加分功能与优化

- [ ] 创建 `weather.js`：`showWeather(cityName, containerSelector)`
- [ ] 天气：调用 OpenWeatherMap 或同类免费 API
- [ ] 天气：选择目的地后显示当地天气 + 动态天气图标
- [ ] 创建 `dark-mode.js`：`initDarkMode(toggleSelector)`
- [ ] 暗黑模式：`body[data-theme="dark"]`，CSS 变量切换
- [ ] 暗黑模式：localStorage 持久化
- [ ] 在 base.css 中补充 `[data-theme="dark"]` 变量覆盖
- [ ] 创建 `lazy-load.js`：`initLazyLoad(imgSelector)`，IntersectionObserver + data-src
- [ ] 对长列表图片应用懒加载
- [ ] 文字闪烁（加分）：`@keyframes blink`，应用于标题/强调文字
- [ ] 省市县联动（加分）：三个 select，省→市→县，静态 JSON 数据
- [ ] 动态密码（加分）：密码输入框 + 显示/隐藏切换按钮
- [ ] 本人形象（加分）：footer 或关于区放置开发者照片
- [ ] 数字人（加分）：iframe 或 SDK 嵌入（按需简化）
- [ ] 数字人背景（加分）：容器 background-image 或 video

---

## 阶段七：Docker 与交付

- [ ] 创建 `Dockerfile`：`FROM nginx:alpine`，COPY 静态资源，COPY nginx.conf
- [ ] 创建 `nginx.conf`：root、index 配置
- [ ] 创建 `docker-compose.yml`：frontend 服务，端口 3205:80
- [ ] 创建 `.dockerignore`：排除 node_modules、.git、.idea 等
- [ ] 创建 `.gitignore`：同上
- [ ] 本地验证：`docker compose up --build`，浏览器访问 http://localhost:3205
- [ ] 编写 `README.md`：技术栈、启动指南、服务地址、功能说明
- [ ] 代码注释：关键函数、复杂逻辑、CSS 块、HTML 模块标注
- [ ] 最终检查：4 个页面功能完整、响应式、无控制台报错

---

## 快速参考

| 文件 | 职责 |
|------|------|
| utils.js | debounce、throttle、scrollToTop、getQueryParam |
| carousel.js | 轮播初始化 |
| nav.js | 导航初始化 |
| back-to-top.js | 回到顶部 |
| search.js | 搜索联想 |
| charts.js | 柱状图、饼图、折线图 |
| weather.js | 天气 API |
| dark-mode.js | 暗黑模式 |
| lazy-load.js | 图片懒加载 |

---

## 修订历史

| 版本 | 日期 | 作者 | 修改说明 |
|------|------|------|----------|
| v1 | 2025-02-15 | jiajing(jiajing@163.com) | 初稿，依据开发设计文档整理 |
